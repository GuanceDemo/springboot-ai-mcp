<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <property name="APP_NAME" value="springboot-ai-mcp"/>
    <property name="LOG_HOME" value="./logs"/>
    <property name="CONSOLE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} %X{dd.service} %X{dd.trace_id} %X{dd.span_id} - %msg%n"/>
    <property name="FILE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} %X{dd.service} %X{dd.trace_id} %X{dd.span_id} - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="com.example.springbootaimcp" level="INFO"/>
    <logger name="org.springframework.ai" level="INFO"/>
    <logger name="org.springframework.ai.tool" level="DEBUG"/>
    <logger name="org.springframework.ai.mcp" level="DEBUG"/>
    <logger name="io.modelcontextprotocol.client" level="DEBUG"/>
    <logger name="io.modelcontextprotocol.client.transport" level="DEBUG"/>
    <logger name="io.modelcontextprotocol" level="INFO"/>
    <logger name="reactor.netty.http.client" level="WARN"/>
    <logger name="org.springframework.web.reactive.function.client" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP_FILE"/>
    </root>

</configuration>
